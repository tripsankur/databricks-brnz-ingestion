# generated_by: pipeline_factory
# spec_id: spec-94139bfa
# spec_version: 8
# DO NOT EDIT — rendered from the approved spec; changes belong in the spec.
# Include from the consuming bundle's databricks.yml.
resources:
  jobs:
    contract_account_recon:
      name: "[contract_account] reconciliation (spec spec-94139bfa v8)"
      tags:
        generated_by: pipeline_factory
        spec_id: spec-94139bfa
        spec_version: "8"
      tasks:
        - task_key: recon
          spark_python_task:
            python_file: ./recon_job.py
          environment_key: default
      environments:
        - environment_key: default
          spec:
            client: "2"
